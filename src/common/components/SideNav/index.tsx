import React, { FunctionComponent } from "react";
import styles from './styles.module.scss';

const SideNav: FunctionComponent = () => {
    return (
        <nav className={styles.navShown}>
        </nav>
    );
}
export default SideNav;